{
  "name": "arena-app",
  "version": "1.0.0",
  "description": "ARENA — تطبيق البطولات الإلكترونية",
  "scripts": {
    "sync": "npx cap sync android",
    "build-android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  }
}
